<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.facebook</groupId>
	<artifactId>UserService</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>

	<properties>
		<java.version>21</java.version>
		<dgs.version>12.0.1</dgs.version>
		<federation-graphql-java-support.version>6.2.1</federation-graphql-java-support.version>
		<graphqlcodegen.version>3.9.0</graphqlcodegen.version>
	</properties>

	<modules>
		<module>shared-proto</module>
		<module>user-service</module>
		<module>socialgraph-service</module>
		<module>user-edge-service</module>
		<module>socialgraph-edge-service</module>
		<module>search-service</module>
	</modules>

	<profiles>
		<!-- Future modules should be added back only when their directories exist. -->
	</profiles>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>com.netflix.graphql.dgs</groupId>
				<artifactId>graphql-dgs-platform-dependencies</artifactId>
				<version>${dgs.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>com.apollographql.federation</groupId>
				<artifactId>federation-graphql-java-support</artifactId>
				<version>${federation-graphql-java-support.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>io.github.deweyjose</groupId>
					<artifactId>graphqlcodegen-maven-plugin</artifactId>
					<version>${graphqlcodegen.version}</version>
				</plugin>
			</plugins>
		</pluginManagement>
	</build>
</project>
